<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet"/>
    <link href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap" rel="stylesheet"/>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/6.4.1/mdb.min.css" rel="stylesheet"/>
    <title>Admin dashboard</title>
</head>
<body>
    <nav class="navbar navbar-light bg-light">
        <div class="container-fluid">
          <a class="navbar-brand">Admin dashboard</a>

          <form class="d-flex input-group w-auto" method="get" action="/admindashboard ">
            <input type="search" class="form-control rounded" placeholder="Search by Username" name="search" aria-label="Search" aria-describedby="search-addon" />
            <span class="input-group-text border-0" id="search-addon">
                <button type="submit" class="btn btn-primary">Search</button>
            </span>
          </form>
          <a href="/adminlogout" class="btn btn-danger" onclick="return confirm('Are you sure you want to log out?');">Logout</a>
        </div>
      </nav>
    <div class="main" style="height: auto; width: 100%;">
        <div class="scrl" style="display: flex;flex-direction: row;flex-wrap: wrap;">
          <% if(users.length > 0){ %>
          <% users.forEach(user => { %>
            <div class="card" style="width: 300px;margin-left: 30px;margin-top: 30px;">
                    <div class="bg-image hover-overlay ripple" data-mdb-ripple-color="light">
                        <img src="/uploads/<%=user.profileImage%>" style="height: 300px;width: 300px;object-fit: cover;"/>
                        <a>
                            <div class="mask" style="background-color: rgba(251, 251, 251, 0.15);"></div>
                        </a>
                    </div>
                    <div class="card-body">
                        <h5 class="card-title">Name : <%= user.username %></h5>
                        <p class="card-text">Address : <%=user.address %></p>
                        <p class="card-text">Phone Number : <%=user.phoneNumber %></p>
                        <a href="/edit/<%= user._id %>" class="btn btn-primary">Edit</a>
                        <a href="/delete/<%= user._id %>" class="btn btn-danger" style="margin-top: 3%; " onclick="return confirm('Are you sure you want to delete this user?');">Delete</a>
                </div>
              </div>
              <% }) %>
              <% } else { %>
                <h1>No users found</h1>
                <% } %>
            </div>
    </div>
</body>
<script>
  type="text/javascript"
  src="https://cdnjs.cloudflare.com/ajax/libs/mdb-ui-kit/6.4.1/mdb.min.js"
</script>
</html>