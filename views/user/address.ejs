<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>View Addresses</title>
    <link rel="stylesheet" href="/css/userprofile.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-alpha1/dist/css/bootstrap.min.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>
</head>
<body> 
    <div class="container rounded bg-white mt-5 mb-5">
        <div class="row">
            <div class="col-md-3 border-right">
                <div class="d-flex flex-column align-items-center text-center p-3 py-5"><img class="rounded-circle mt-5" width="150px" height="150px" style="object-fit: cover;" src="<%=user.profileImage%>"><span class="font-weight-bold"><%=user.username%></span><span class="text-black-50"><%=user.email%></span><span> </span></div>
            </div>
            <div class="col-md-5 border-right">
                <div class="p-3 py-5">
                    <div class="d-flex justify-content-between align-items-center mb-3">
                        <h4 class="text-right">Manage Address</h4>
                    </div>
                    <% for (let i = 0; i < addresses.length; i++) { %>
                    <div class="card" style="margin-bottom: 6%;">
                        <h5 class="card-header"><%= addresses[i].type %></h5>
                        <div class="card-body">
                          <h5 class="card-title"><%= addresses[i].name %></h5>
                          <p class="card-text"><%= addresses[i].houseName %></p>
                          <p class="card-text"><%= addresses[i].street %></p>
                          <p class="card-text"><%= addresses[i].city %></p>
                          <p class="card-text"><%= addresses[i].pinCode %></p>
                          <p class="card-text"><%= addresses[i].state %></p>
                          <p class="card-text"><%= addresses[i].phone %></p>
                          <div class="btns" style="display: flex; justify-content: space-around;">
                              <form action="/removeAddress/<%= addresses[i]._id %>" method="POST">
                                <button type="submit" class="btn btn-danger" style="width: 100px;" onclick="return confirm('Are you sure you want to delete this address?');">Delete</button>
                            </form>
                            <form action="/editAddress/<%= addresses[i]._id %>" method="get">
                                <button type="submit" class="btn btn-primary" style="width: 100px;">Edit</button>
                            </form>
                          </div>
                        </div>
                      </div>
                      <% } %>
                </div>
            </div>
            <div class="col-md-4">
                <div class="p-3 py-5">
                    <div class="col-md-12"><a href="/addaddress/<%=user._id%>" class="btn btn-primary" style="width: 300px;">Add address</a></div> <br>
                    <div class="col-md-12"><a href="#" class="btn btn-info" style="width: 300px;">My Coupons</a></div> <br>
                    <div class="col-md-12"><a href="#" class="btn btn-info" style="width: 300px;">My Orders</a></div> <br>
                    <div class="col-md-12"><a href="#" class="btn btn-info" style="width: 300px;">My Reviews & Rating</a></div> <br>
                </div>
            </div>
        </div>
    </div>
</body>
</html>